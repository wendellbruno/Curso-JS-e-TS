

function calculadora(peso, altura){
    const peso = document.querySelector('peso');
    const altura = document.querySelector('altura');
    const resultado = (peso*altura) / peso;

    
    alert(resultado);
}